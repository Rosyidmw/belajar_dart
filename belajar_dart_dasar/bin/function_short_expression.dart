// Menggunakan cara return
int sum(int pertama, int kedua) {
  return pertama + kedua;
}

// Menggunakan cara short
int jumlah(int fisrt, int second) => fisrt + second;

void main() {
  print(sum(15, 5));

  print(jumlah(10, 6));
}